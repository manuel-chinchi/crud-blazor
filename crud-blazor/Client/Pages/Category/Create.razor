@page "/category-create"
@inject HttpClient Http
@inject NavigationManager Navigation 
 
<h3>Create category</h3>
<br />

<EditForm Model="category" OnValidSubmit="CreateCategory">
    <div class="form-group">
        <label>Name</label>
        <InputText class="form-control" @bind-Value="category.Name"></InputText>
    </div>
    <br />
    <a class="btn btn-secondary" href="categories"><span class="oi oi-chevron-left mr-2"></span>Back</a>
    <button type="submit" class="btn btn-success"><span class="oi oi-check mr-2"></span>Accept</button>
</EditForm>

@code {
    public Category category = new Category();


    async Task CreateCategory()
    {
        await Http.PostAsJsonAsync("Category", category);
        Navigation.NavigateTo("categories");
    }
}
